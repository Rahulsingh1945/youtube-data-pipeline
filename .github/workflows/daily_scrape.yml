name: Daily YouTube Scraper

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run scraper
        run: |
          python scripts/youtube_metadata.py

      - name: Validate data
        run: |
          python scripts/validate_data.py

      - name: Clean data
        run: |
          python scripts/clean_data.py

      - name: Save updated dataset
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          git add data/raw/youtube_dataset.csv data/processed/youtube_clean.csv

          git commit -m "Daily dataset update" || echo "No changes"

          git push